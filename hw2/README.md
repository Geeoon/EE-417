# Instructions to run
1. Run `python3 -m pip install requirements.txt` to install dependencies.
2. Run `python3 test_main.py` to run the program.
3. An example image and transmission is shown.
4. Close the windows until the simulation runs.
5. The results appear after a while.

