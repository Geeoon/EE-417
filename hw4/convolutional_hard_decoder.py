import numpy as np

def convolutional_hard_decoder(symbols: np.ndarray, G: list[list[int]]=[[0o5, 0o7]]) -> np.ndarray:
    num_states = 2 ** len(G[0])

    return num_states

print(convolutional_hard_decoder(None))
